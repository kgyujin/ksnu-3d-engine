using UnityEngine;

public class Wand_item : MonoBehaviour
{
    // Start is called once before the first execution of Update after the MonoBehaviour is created
    public float raycast_distance = 10f;
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
